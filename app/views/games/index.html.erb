<h1>Games</h1>

<% @games.each do |game| %>
<ul>
  <li>
    <%= link_to image_tag(game.photo), game_path(game) %><br/>
    Title: <%= game.title %><br/>
    Description: <%= game.description %><br/>
    Rating: <%= game.rank %>
  </li>
</ul>
<% end %>